#include<stdio.h>
void main()
{
    int a,b,sum,avg;
    printf("enter the value for summation and avegrage of number a,b:-");
    scanf("%d%d",&a,&b);
    sum=a+b;
    avg=a+b/2;
    printf("the sum of the numbers is:-%d",sum);
    printf("\nThe average of the numbers is%d",avg);
}
