#include <stdio.h>
int main()
{
    int a,b,sum;
    printf("Enter first number and second number",a,b);
    scanf("%d%d",&a,&b);
    sum =a+b;
    printf("THE SUMATION IS:-%d",sum);
    return 0;
}
